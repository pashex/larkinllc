= form_tag sessions_path, class: "form-inline", role: "form" do
  = email_field_tag :email, params[:email], required: true, placeholder: User.human_attribute_name(:email)
  = password_field_tag :password, nil, required: true, placeholder: User.human_attribute_name(:password)
  = submit_tag t('.enter')
